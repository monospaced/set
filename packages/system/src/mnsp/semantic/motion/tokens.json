{
  "$schema": "../../../../schemas/2025.10/schema/format.json",
  "duration": {
    "0": {
      "$description": "Instantaneous duration.",
      "$value": "{primitive.motion.duration.0}"
    },
    "50": {
      "$description": "Micro duration step.",
      "$value": "{primitive.motion.duration.50}"
    },
    "100": {
      "$description": "Default duration. Pick the step that matches the size and emphasis of the motion.",
      "$value": "{primitive.motion.duration.100}"
    },
    "150": {
      "$description": "Quick duration step.",
      "$value": "{primitive.motion.duration.150}"
    },
    "200": {
      "$description": "Moderate duration step.",
      "$value": "{primitive.motion.duration.200}"
    },
    "250": {
      "$description": "Comfortable duration step.",
      "$value": "{primitive.motion.duration.250}"
    },
    "300": {
      "$description": "Extended duration step.",
      "$value": "{primitive.motion.duration.300}"
    },
    "600": {
      "$description": "Long duration step.",
      "$value": "{primitive.motion.duration.600}"
    },
    "3000": {
      "$description": "Ambient duration step.",
      "$value": "{primitive.motion.duration.3000}"
    },
    "$type": "duration"
  },
  "easing": {
    "$type": "cubicBezier",
    "easeIn": {
      "$description": "Ease-in curve. Use for elements leaving the viewport — slow start, accelerating exit.",
      "$value": "{primitive.motion.easing.easeIn}"
    },
    "easeInOut": {
      "$description": "Ease-in-out curve. Use for movements that both originate and settle on screen (e.g. expansions in place).",
      "$value": "{primitive.motion.easing.easeInOut}"
    },
    "easeOut": {
      "$description": "Ease-out curve. Use for elements entering the viewport — fast start, gentle settle.",
      "$value": "{primitive.motion.easing.easeOut}"
    },
    "emphasized": {
      "$description": "Emphasised easing curve. Use for transitions that should feel more expressive (e.g. brand-led page transitions, hero reveal motion).",
      "$value": "{primitive.motion.easing.emphasized}"
    }
  }
}
