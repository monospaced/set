# @measured/set-react

React adapter for Set. Typed React wrappers over the core custom-element library.

Wrappers are generated from the structured `SET_*_SPEC` contract in `@measured/set-core` via the `@measured/set-adapter` walker, so the React surface always tracks the underlying component IR.
