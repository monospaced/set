// Generated by @measured/set-adapter — do not edit directly.
// Regenerate with `pnpm react:generate` (runs automatically on `pnpm react:build`).
import { buildSetPoster, type SetPosterProps } from "@measured/set-core";
import type { ReactNode } from "react";

import {
  type NativeAttrsFor,
  pickNativeExtras,
  reactify,
} from "../../reactify";

const SLOT_POSTER_MEDIA = "__SET_SLOT_POSTER_MEDIA__";
const SLOT_POSTER_CHILDREN = "__SET_SLOT_POSTER_CHILDREN__";

export type PosterProps = Omit<SetPosterProps, "media" | "children"> & {
  media: ReactNode;
  children?: ReactNode;
} & NativeAttrsFor<HTMLDivElement>;

export function Poster(props: PosterProps): ReturnType<typeof reactify> {
  const { children, contentTheme, id, media, surface, ...rest } = props;
  const hasChildren = children != null && children !== false;
  const node = buildSetPoster({
    children: hasChildren
      ? (SLOT_POSTER_CHILDREN as unknown as SetPosterProps["children"])
      : undefined,
    contentTheme,
    id,
    media: SLOT_POSTER_MEDIA as unknown as SetPosterProps["media"],
    surface,
  });
  return reactify(
    node,
    pickNativeExtras(rest as unknown as Record<string, unknown>),
    {
      [SLOT_POSTER_MEDIA]: media,
      ...(hasChildren ? { [SLOT_POSTER_CHILDREN]: children } : {}),
    },
  );
}
