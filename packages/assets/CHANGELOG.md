# @measured/set-assets

## 0.4.3

## 0.4.2

## 0.4.1

## 0.4.0

## 0.3.0

## 0.2.0

### Minor Changes

- c9c9ef6: Add favicon assets — `favicon.ico`, `favicon.svg` (light/dark adaptive via `prefers-color-scheme`), `apple-touch-icon.png`, and maskable PWA icons at 192px and 512px. Each is exposed via its own `./favicons/<name>` export path. See package README for `<head>` markup and PWA manifest usage.

### Patch Changes

- b00bb58: Add concise `description` to each public package's `package.json` and align the README's first paragraph to match. The descriptions are what npm shows in list views; they were previously empty across the repo, leaving npm to fall back to whatever appeared in the README first line.

## 0.2.0-alpha.6

## 0.2.0-alpha.5

## 0.2.0-alpha.4

## 0.2.0-alpha.3

## 0.2.0-alpha.2

### Minor Changes

- c9c9ef6: Add favicon assets — `favicon.ico`, `favicon.svg` (light/dark adaptive via `prefers-color-scheme`), `apple-touch-icon.png`, and maskable PWA icons at 192px and 512px. Each is exposed via its own `./favicons/<name>` export path. See package README for `<head>` markup and PWA manifest usage.

## 0.1.1-alpha.1

## 0.1.1-alpha.0
