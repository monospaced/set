---
"@monospaced/set-core": minor
---

Add the `image` icon to the curated set.

Declares `image` in `TDESIGN_ICON_NAMES` and regenerates `icons.generated.ts`
so the icon ships in the bundled node registry.
